import { AutofocusDirective } from './autofocus.directive';

describe('AutofocusDirective', () => {
  it('should create an instance', () => {
    let a;
    const directive = new AutofocusDirective(a);
    expect(directive).toBeTruthy();
  });
});
